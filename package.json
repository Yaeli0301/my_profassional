{
  "name": "my-professional",
  "version": "1.0.0",
  "description": "Full stack application for My Professional",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client/my_proffssional && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client/my_proffssional && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
     
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@mui/x-date-pickers": "^7.27.1",
    "formik": "^2.4.6",
    "react-router-dom": "^7.2.0",
    "yup": "^1.6.1"
  }
}
